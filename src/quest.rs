#[derive(Clone)]
pub struct Quest {
    name: String,
    // kind: QuestKind,
    // status: Status,
    link: String,
    description: String,
}
/*
#[derive(Clone)]
pub enum QuestKind {
    Daily,
    Special,
}

#[derive(Clone)]
pub enum Status {
    Pending,
    InProgress,
    Done,
}
*/
